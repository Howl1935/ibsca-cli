import { Terraform } from "./terraform/Terraform";

export const languages = [
    {
        className : ["."],
        extension : "."
    },

    {
        className : ["Terraform", Terraform],
        extension : "tf"
    }

]